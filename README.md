# SIEM Pipeline - Dahfa Edition 🔥

Stack:
- Kafka (KRaft)
- Wazuh Indexer
- Python Producer
- Python Backend Consumer

## Setup

```bash
sudo apt install python3 python3-venv git
git clone <repo-url>
cd siem-pipeline
python3 -m venv siem-env
source siem-env/bin/activate
pip install -r requirements.txt



---

# 🎯 STEP 6 — Init Git

Masuk folder repo:

```bash
cd /home/dahfa/siem-pipeline
git init
git add .
git commit -m "Initial SIEM pipeline backup"



